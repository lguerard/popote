# Base de données
POSTGRES_PASSWORD=popote

# LLM local (Ollama)
# qwen2.5:7b (~5 Go) tient entièrement sur un GPU de 10 Go avec la fenêtre
# de 8192 tokens. qwen2.5:14b (~9 Go + ~1,5 Go de contexte) n'y tient pas :
# Ollama fait alors tourner une partie du modèle sur le processeur, et
# l'analyse d'une recette dépasse le délai de 5 minutes. Le backend le
# signale dans ses logs (« déborde du GPU »). Le 14b n'a d'intérêt qu'avec
# un GPU d'au moins 12 Go. scripts/model_watch/ compare des modèles sur de
# vraies recettes.
OLLAMA_MODEL=qwen2.5:7b
# Fenêtre de contexte (tokens). En dessous de ~8192 les pages longues sont
# tronquées ; au-dessus, la VRAM consommée augmente.
OLLAMA_NUM_CTX=8192

# LLM cloud (optionnel — si défini, Claude est utilisé à la place d'Ollama)
CLAUDE_API_KEY=

# Whisper (transcription vidéo)
WHISPER_MODEL=large-v3
WHISPER_DEVICE=cuda   # cuda | cpu
# Modèle utilisé si le GPU est indisponible (large-v3 est trop lent sur CPU).
WHISPER_CPU_MODEL=small
# Décharge le modèle Ollama du GPU avant chaque transcription (rechargé
# ensuite automatiquement) : sans ça, Whisper ne tient pas sur le GPU.
WHISPER_FREE_GPU=true

# App
# Signe les jetons de session. Le backend REFUSE de demarrer si elle est
# absente, trop courte (32 caracteres minimum) ou laissee par defaut :
# une cle devinable laisse fabriquer un jeton valide pour n'importe quel
# compte. Generation :
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=

# Autorise les DEMANDES de compte. Aucune demande n'est accordee sans
# validation d'un administrateur depuis la page Comptes : true ouvre le
# formulaire, pas l'acces.
#
# Le tout premier compte fait exception -- il n'y a alors personne pour
# valider : il est approuve d'office, devient administrateur, et adopte
# les recettes existantes. Mets donc true, cree TON compte, et laisse
# ensuite true si tu attends d'autres demandes.
ALLOW_SIGNUP=false
PORT=80

# Génération d'image IA (imagegen/) : vignette de secours quand une
# recette n'a pas de photo et qu'aucune n'est envoyée manuellement.
# Tourne dans son propre conteneur (voir imagegen/README.md) pour ne
# jamais entrer en conflit avec les bibliothèques CUDA du backend.
# sdxl-turbo : nettement meilleur que sd-turbo (~7 Go de VRAM en fp16).
# Serveur sans GPU ou avec peu de RAM (< 16 Go) : stabilityai/sd-turbo.
IMAGE_GEN_MODEL=stabilityai/sdxl-turbo
IMAGE_GEN_DEVICE=cuda   # cuda | cpu
# Décharge le modèle Ollama du GPU le temps de générer une image (il est
# rechargé en quelques secondes à la prochaine extraction). false si le GPU
# a assez de mémoire pour les deux.
IMAGE_GEN_FREE_GPU=true

# Surveillance mensuelle des modèles (scripts/model_watch/, voir son README)
# — optionnel : sans ces variables, le rapport est seulement enregistré sur
# disque dans scripts/model_watch/reports/, pas envoyé par email.
MODEL_WATCH_SMTP_HOST=
MODEL_WATCH_SMTP_PORT=587
MODEL_WATCH_SMTP_USER=
MODEL_WATCH_SMTP_PASSWORD=
MODEL_WATCH_SMTP_FROM=
MODEL_WATCH_SMTP_TO=
MODEL_WATCH_SMTP_USE_TLS=true
# Marge de sécurité (Go) retranchée à la VRAM totale mesurée pour obtenir
# le budget des modèles candidats.
MODEL_WATCH_VRAM_HEADROOM_GB=1.0
# Nombre de modèles candidats testés par run (chacun peut peser plusieurs
# Go à télécharger — garder ce nombre bas si la bande passante est limitée).
MODEL_WATCH_MAX_CANDIDATES=3
# Écart de score minimal (sur 100) pour qu'un candidat soit jugé "vraiment
# meilleur" et remplace le modèle actuel, plutôt qu'un bruit de mesure.
MODEL_WATCH_MIN_IMPROVEMENT=5.0
