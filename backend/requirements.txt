fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
# yt-dlp non épinglé : YouTube casse régulièrement les vieilles versions
yt-dlp
faster-whisper==1.0.3
playwright==1.48.0
beautifulsoup4==4.12.3
anthropic==0.37.1
python-multipart==0.0.12
aiofiles==24.1.0
pillow==10.4.0
pytesseract==0.3.13
# faster-whisper 1.0.3 declare huggingface_hub sans borne haute : un build
# neuf attrape la 1.x, qui a retire les API deprecies dont il depend encore.
huggingface_hub<1.0
# CTranslate2 est le moteur de faster-whisper. Non epingle, un build neuf
# peut changer la version majeure de cuDNN attendue : on fige le couple.
ctranslate2==4.5.0
# Sur GPU, CTranslate2 charge libcublas.so.12 et libcudnn. Le NVIDIA
# Container Toolkit n'injecte que le PILOTE (libcuda), jamais ces
# bibliotheques CUDA : elles doivent venir de l'image. D'ou ces roues pip
# et le LD_LIBRARY_PATH correspondant dans le Dockerfile. Sans elles :
# "library libcublas.so.12 is not found or cannot be loaded".
nvidia-cublas-cu12==12.4.5.8
nvidia-cudnn-cu12==9.1.0.70
# Comptes utilisateurs : bcrypt pour les mots de passe, PyJWT pour les
# jetons de session (voir app/core/security.py).
bcrypt==4.2.1
PyJWT==2.10.1
